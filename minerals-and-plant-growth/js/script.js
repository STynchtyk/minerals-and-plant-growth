const slides=[...document.querySelectorAll('.slide')];const dots=document.querySelector('.dots');let current=0;slides.forEach((s,i)=>{const b=document.createElement('button');b.className='dot';b.setAttribute('aria-label','Go to slide '+(i+1));b.addEventListener('click',()=>show(i));dots.append(b)});function show(i){current=Math.max(0,Math.min(slides.length-1,i));slides.forEach((s,n)=>{s.hidden=n!==current;s.classList.toggle('active',n===current)});[...dots.children].forEach((d,n)=>d.setAttribute('aria-current',String(n===current)));document.querySelector('#count').textContent=String(current+1).padStart(2,'0')+' / 06';document.querySelector('#prev').disabled=current===0;document.querySelector('#next').disabled=current===5}document.querySelector('#prev').onclick=()=>show(current-1);document.querySelector('#next').onclick=()=>show(current+1);document.addEventListener('keydown',e=>{if(e.key==='ArrowRight'){e.preventDefault();show(current+1)}if(e.key==='ArrowLeft'){e.preventDefault();show(current-1)}if(e.key==='Home')show(0);if(e.key==='End')show(5)});let startX=0,startY=0;document.querySelector('main').addEventListener('touchstart',e=>{startX=e.changedTouches[0].clientX;startY=e.changedTouches[0].clientY},{passive:true});document.querySelector('main').addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-startX,dy=e.changedTouches[0].clientY-startY;if(Math.abs(dx)>60&&Math.abs(dx)>Math.abs(dy))show(current+(dx<0?1:-1))},{passive:true});show(0);

const fullButton=document.querySelector('#fullscreen');const fullStatus=document.querySelector('#screen-status');const screenRoot=document.documentElement;const requestFull=screenRoot.requestFullscreen||screenRoot.webkitRequestFullscreen;const leaveFull=document.exitFullscreen||document.webkitExitFullscreen;function isFull(){return Boolean(document.fullscreenElement||document.webkitFullscreenElement)}function updateFull(){const active=isFull();fullButton.textContent=active?'Exit full screen':'Full screen';fullButton.setAttribute('aria-pressed',String(active))}if(!requestFull||!leaveFull){fullButton.disabled=true;fullButton.title='Full screen is not supported in this browser';fullStatus.textContent='For full screen, open this file in Chrome or Edge.'}else{fullButton.addEventListener('click',async()=>{fullStatus.textContent='';try{if(isFull())await leaveFull.call(document);else await requestFull.call(screenRoot);updateFull()}catch{fullStatus.textContent='Full screen is unavailable here. Open this file in Chrome or Edge.'}});document.addEventListener('fullscreenchange',updateFull);document.addEventListener('webkitfullscreenchange',updateFull)}
